#Previous knowledge testing
infoA <- c(7.5, 3.8, 9.6)
infoA

#Vector with names
nameVector <- c("Paradiso", "Dracula", "Shawshank")

#Add names to infoA
names(infoA) <- nameVector
infoA

#Creating a matrix
spremenljivka <- matrix(1:54, byrow = TRUE, ncol = 6)
spremenljivka
